language : python
python:
    - "2.7"
services:
    - "couchdb"

before_script:
    - curl -X PUT localhost:5984/config

install:
    pip install couchdbkit couchapp

script:
    python -m unittest discover -v
